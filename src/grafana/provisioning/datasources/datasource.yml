apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Uses the Prometheus container name and internal port
    url: https://prometheus:9090
    isDefault: true
