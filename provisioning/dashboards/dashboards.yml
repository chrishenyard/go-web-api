apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10 # Hot-reloads changes to JSON files every 10 seconds
    options:
      path: /etc/grafana/provisioning/dashboards
